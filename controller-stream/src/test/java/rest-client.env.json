{
  "development": {
    "host": "localhost",
    "port": 8888,
    "example_node_project": "/init?runtime=node_lambda&project_name=node-test",
    "health": "/health",
    "push": "/push"
  },

  "production": {
    "host": "localhost",
    "port": 80,
    "example_node_project": "/init?runtime=node_lambda&project_name=node-test",
    "health": "/health",
    "push": "/push"
  }
}